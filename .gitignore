# 런타임/대용량 산출물 (리포에 넣지 않음)
.venv/
models/
__pycache__/
*.pyc
*.pyo

# 로그
*.log
stdout.log
stderr.log
caddy.stdout.log
caddy.stderr.log

# 백업본
*.bak
*.bak.*
web/*.bak
web/*.bak.*

# IDE / OS
.idea/
.DS_Store

# 로컬 비밀/환경 (혹시 생기면)
.env
.env.*

# 실행 시 생성되는 데이터 (스크립트로 재생성)
data/samples/
data/uploads/
data/originals/

# 반입 수집물 (USB로 이동 — 리포에 넣지 않음)
transfer_out/
*.bundle

# 실데이터(개인의료정보) — 깃 반입 금지
*_OCR_*.txt
data/scans_txt/
data/clean_pdf/
data/originals/
