psycopg[binary]
FlagEmbedding      # bge-m3 (운영 임베딩)
requests
fastapi
uvicorn
openpyxl           # db/extract_seed.py (엑셀->시드)
reportlab          # 심의의결서 PDF export (services/decision_doc.py)
sqlglot            # services/stats.py Text-to-SQL AST 검증 (순수 파이썬, 폐쇄망 반입 용이)
pypdf              # scripts/extract_pdf_corpus.py (페이지 보존 PDF 추출)
pymupdf            # scripts/ocr_ingest_scans.py (스캔 PDF 렌더링·텍스트층 추출)
pytesseract        # scripts/ocr_ingest_scans.py (OCR — 시스템에 tesseract-ocr+kor 필요)
pillow             # OCR 이미지 처리
pytest             # tests/ 회귀 스위트 (개발용 — 운영 서버 필수 아님)
